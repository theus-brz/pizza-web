import styled from 'styled-components';

import { InputWrapper, InputField } from '~/styles/components';

export const MaskedInputWrapper = styled(InputWrapper)``;

export const MaskedInputField = styled(InputField)``;
