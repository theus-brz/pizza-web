import styled from 'styled-components';

import { InputWrapper, InputField } from '~/styles/components';

export const FormInputWrapper = styled(InputWrapper)``;

export const FormInputField = styled(InputField)``;
